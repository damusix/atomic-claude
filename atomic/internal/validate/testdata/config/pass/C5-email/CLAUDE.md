## Project context

Platform admin: `admin@concierge.test` at `/admin`. Production
(`example.com`) has no demo accounts — only `alice@`/`bob@example.com`
as platform admins. Reach the team at team@example.org.

## Signals

A real ref that must still resolve: @.claude/project/signals.md
