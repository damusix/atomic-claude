# {{SLUG}} subtree

This directory holds the subtopics, code, and assets for the `{{SLUG}}`
topic. The sibling `{{SLUG}}.md` is the router summary — read it first, then
descend into whichever subtopic file is relevant. One topic per file inside
this subtree too.
