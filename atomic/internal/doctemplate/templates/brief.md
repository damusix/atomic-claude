<!--
Template for $SCRATCH/BRIEF.md — emitted by `atomic template brief`. The thin orchestrator-curated brief handed to every
fresh-context subagent in the implement→review loop (/subagent-implementation,
/atomic-plan spec loop, /subagent-diagnose). Copy this body, fill every <angle-bracket>
placeholder, delete this comment. Orchestrator-owned: refreshed each iteration —
overwrite, don't append.
/subagent-diagnose variant: replace the **Spec:** line with a source-pointer section
targeting $SCRATCH/CONTEXT.md (branch, head SHA, base SHA, failure summary, suspected
surface area), and append the investigator's output as `## Phase 1 — surface map`.
-->
# Brief: <topic>

**Spec:** `docs/spec/<topic>.md` (canonical source — read this first)

**Iteration scope (this turn):** <which checkpoint from the spec>

**Reviewer feedback to address:** <findings from prior iteration, or "N/A — first iteration">

**Success criteria for this iteration:**
- <criterion>
- <criterion>

**Base SHA for diff:** <git rev-parse HEAD>
