---
type: Domain
description: Local read-only HTTP server with Obsidian-style shell, goldmark render, in-body wikilink resolution, a cosmos.gl-powered Docs|Code graph pane (Cytoscape mini-graph in the rail), federated code search, code-file modal, and SSE-driven live-reload for the nav tree and open page.
---

# serve

## What it does

`atomic serve [path] [--port N] [--open]` starts a localhost-only, read-only HTTP server (default port 4500) that renders a wiki realm or a single repo as a navigable graph in the browser. Scope is resolved via `realm.Resolve`: registered realm root → Realm scope, inside a member → Member scope, bare repo → Repo scope. Binds to `127.0.0.1` only; no write operations of any kind. Shuts down cleanly on SIGINT/SIGTERM. A shared realm-snapshot store backs live-reload: `GET /events` streams realm-change notifications over Server-Sent Events so the nav tree and the currently displayed page/rail refresh automatically, without a manual reload or a full restart.

The UI is a single persistent Obsidian-style shell: top bar (breadcrumb + `md|code` search), left nav tree, middle content pane with `[page|system]` toggle, right rail (YAML properties + this-page mini-graph + OUT/IN link lists). Toggling to system mode navigates to the URL-addressable `/graph` page, which hosts two views behind a nested Docs|Code control: the whole-realm docs graph (cosmos.gl over `GET /graph/data`) and a per-repo code graph (cosmos.gl over `GET /code/graph/data`), sharing one `graph-core.js` cosmos.gl engine. Markdown page rendering uses `RenderMarkdownWithGraph`, which resolves Obsidian-style `[[page]]` / `[[page|alias]]` wikilinks in the body as in-shell htmx navigations using the same graph edges as the right rail; broken wikilinks render as `<span class="wikilink-broken">`.

## Artifacts

- [`docs/spec/atomic-serve.md`](../../docs/spec/atomic-serve.md) — implementation spec (success criteria SC1–SC11, non-goals, security contract)
- [`docs/design/atomic-serve.md`](../../docs/design/atomic-serve.md) — design deliberation (scope model, asset vendoring decisions, route map)
- [`docs/reference/serve.md`](../../docs/reference/serve.md) — user-facing reference: flags, scope table, all routes and views
- [`docs/design/serve-live-reload.md`](../design/serve-live-reload.md) — design deliberation for live-reload: problem statement (three independent, per-request-or-startup-frozen filesystem walks giving nav/graph/rail three different freshness models), goals/non-goals (no fsnotify, no WebSockets, whole-realm fingerprint as the unit of change, no user-facing tick-interval flag), chosen shape (subscriber-gated ticker + one shared snapshot store + plain-`EventSource` SSE + lazy per-request fingerprint validation as a correctness backstop)
- [`docs/spec/serve-live-reload.md`](../spec/serve-live-reload.md) — implementation spec, checkpoints CP1–CP5; CP5 (system-mode graph diff/patch) was struck and superseded at the cosmos engine merge (see the 2026-07-08 change-log entry) — server-side flows (snapshot store, `/events`, page-mode reconcile) are unaffected and remain current

## CLI code

- [`atomic/internal/serve/`](../../atomic/internal/serve) — leaf package; 58 Go files at top level (plus [`assets/`](../../assets) and [`templates/`](../../templates)). Key files:
  - `serve.go` — `Run(args, stdout, stderr) int` (os.Exit entry), `RunWithContext(ctx, opts) int` (testable entry), `Options` struct, `DisplayScope` enum (`DisplayScopeRepo / Realm / Member`), `ResolveDisplayScope`; `parseFlags` normalizes the positional target-dir arg to an absolute path via `filepath.Abs`; [`/`](../..) root serves the Obsidian shell template; `/healthz` liveness probe; `/status` realm-health dashboard (was `/health` in CP6); registers `/code/graph/data` → `NewCodeGraphHandler` and `/code/graph/members` → `NewCodeGraphMembersHandler`; `RunWithContext` constructs exactly one `*snapshotStore` via `NewSnapshotStore(navRoot)` (synchronously warmed) and one `subscriberRegistry` via `newSubscriberRegistry()`, shares the store across the `/nav`, `/page/`, `/rail/`, `/events`, and `/graph/data` handler constructors so they read one walk instead of each tracking its own, registers `/events` → `NewEventsHandler(store, eventsRegistry)`, and calls `startTicker(ctx, store, eventsRegistry, store.tickInterval)` bound to the same context that drives graceful shutdown so the ticker goroutine never outlives the server
  - `render.go` — goldmark + chroma markdown-to-HTML renderer; mermaid fenced-block pass-through. Three public entry points: `RenderMarkdown` (bare), `RenderMarkdownWithLinks` (link rewriting), `RenderMarkdownWithGraph` (link rewriting + in-body wikilink resolution via the page's graph edges). `renderMarkdown` is the shared private implementation; `chroma` style is `monokai`; when `wikiResolve` is non-nil it wires `wikilinkInlineParser` at priority 150 and `wikilinkRenderer`; YAML frontmatter is stripped before goldmark sees it (prevents spurious `<hr>` + setext `<h2>`); htmx history-cache-miss restores (`HX-History-Restore-Request: true`) receive the full shell document, not a fragment, so Back/Forward does not destroy the nav shell. `NewPageHandler` is marked deprecated in source; `NewPageHandlerWithGraph` is the production entry point. `chromaHighlight` and `chromaHighlightLines` no longer fall back to `lexers.Analyse` (content-based guessing) — unknown or absent fence language falls through to `lexers.Fallback` (plaintext, no token colors), preventing mis-detection of terminal transcripts and plain-text files as code.
  - `wikilink.go` — goldmark inline parser + renderer for Obsidian-style `[[page]]` / `[[page|alias]]` wikilinks. `wikilinkInlineParser` triggers on `[`, commits only on `[[…]]` syntax, advances the reader by `2+close+2` bytes. `wikilinkResolverFromGraph` derives a `wikilinkResolver` from the focused page's outbound `Graph.Outbound` edges filtered to `mdlink.Wikilink` kind — reuses the resolution computed in `graph.go` so the body and the right rail can never disagree. `wikilinkRenderer` emits: resolved → `<a class="wikilink" hx-get="/page/…" hx-target="#main-pane">` (or `wikilink wikilink-ambiguous` class); broken → `<span class="wikilink-broken">`.
  - `nav.go` — left-nav tree builder; realm scope renders six groups (Realm / Repos / Concerns / Knowledge / Buckets / External); repo scope renders docs file tree + Code placeholder; stale/diff badges via injectable `StalenessFn`; `computeStaleness` production default calls `wiki.Stale` once per request; `memberLinkRel` routes to `SummaryPath`, then `SignalsPath`, then member dir — never guesses `wiki/repos/<name>.md` to avoid 404s on non-summarized members; bucket folders are collapsible `<details>` trees collapsed by default; repo-scope's file list is read from the shared `*snapshotStore` (`NavOptions.Store`) via `store.ensureFresh()` — `NewNavHandler` builds a private one-off store rooted at `RealmRoot` when `Store` is nil (e.g. direct-construction tests), so repo-scope nav still works without a shared walk; `sseLiveParam` (`?live=1`, checked via `isSSETriggered`) marks a live-reload-triggered `/nav` refetch so the handler skips `computeStaleness` — a git-subprocess-backed check — on that request; an ordinary navigation request still runs it
  - `graph.go` — `BuildLinkGraph` walks all `*.md` files under root, extracts links via `mdlink.ExtractLinks`, and resolves wikilinks using nearest-then-alphabetical rule; populates `nodeTypes map[string]string` by calling `resolveNodeType(relPage, data)` for each page during the Step 3 extract loop (file content already in memory, no second read); `resolveNodeType` resolution order: (1) frontmatter `type` key (case-insensitive, mapped via `frontmatterTypeToClass`), (2) path-convention fallback (`/repos/` → "repo", `/concerns/` → "concern", `/knowledge/` → "knowledge"), (3) default "page"; `frontmatterTypeToClass` maps: "knowledge"→"knowledge", "concern"→"concern", "repo summary"→"repo", "repo"→"repo", "bucket"→"bucket"; `NodeType(relPath)` method returns the cached type (non-.md nodes not in `nodeTypes` default to "external"); `resolveRootRelative` is a shared helper called by BOTH `resolveMarkdownLink` (for leading-slash / OKF §5.1 form links) AND `resolvePageHref` (render-time link rewriter) — strips the leading slash, cleans, traversal-guards, then probes, ensuring the two functions resolve bundle-root-relative `/path.md` links identically; `Edge` struct with `Broken`, `Ambiguous`, `CodeFile` (non-.md source files rendered as `/file/` links rather than broken), `External` flags; `Graph` carries `nodeSet map[string]bool` for O(1) `Has()` membership; `resolveDirIndex` probes [`README.md`](../../README.md) / [`readme.md`](../../readme.md) / `index.md` / [`docs/wiki/index.md`](index.md) in that order; test coverage: `graph_resolvepagehref_internal_test.go` (179 lines, internal package tests for `resolvePageHref`) and `nodetype_test.go` (203 lines testing `resolveNodeType` and `NodeType`)
  - `snapshot.go` (NEW, live-reload) — the realm-snapshot store: `realmSnapshot` is an immutable `{fp, navPaths, graph, manifest}` struct published behind one `atomic.Pointer` swap so a reader can never observe a torn read (new fingerprint paired with a stale graph); `ensureFresh()` is the single accessor shared by the ticker, every per-request read (nav/page/rail/graph-data), and the startup warm — it runs a cheap, quiet-window-filtered, stat-only `fingerprint()` walk (no file content read) and only calls the expensive `rebuild()` (`BuildLinkGraph`, content reads) when the fingerprint differs from the published snapshot; concurrent callers that all observe staleness collapse to one rebuild via a non-blocking `rebuilding atomic.Bool` CAS gate — losers return the current (possibly one-tick-stale) snapshot rather than block, since the dedup key is the gate, not the fingerprint value (two walkers racing a changing filesystem can legitimately compute different fingerprints for the same moment); `NewSnapshotStore(root)` is the exported, synchronously-warmed production constructor `RunWithContext` calls once and shares across every handler; `diffManifest` computes the added/edited/removed relpath list a rebuild reports
  - `events.go` (NEW, live-reload) — the `GET /events` SSE endpoint and its subscriber-gated ticker: `subscriberRegistry` (subscribe/unsubscribe/broadcast/count); each `subscriber` is a buffered-1 coalescing channel whose `push` overwrites rather than blocks when a reader hasn't drained the previous event, so a slow or dead subscriber never stalls `broadcast`'s caller (the ticker); `changeEvent{FP, Changed}` is the wire payload — `Changed` is capped at `changedCap` (100 entries) and omitted (`omitempty`) above the cap, so the client treats a missing field the same as an oversized one ("refetch everything"); `NewEventsHandler` registers a subscriber, immediately pushes a resync event reflecting current on-disk state regardless of the tick cycle, then streams subsequent broadcasts from the subscriber's slot until the request context ends, with a `sseWriteDeadline` (5s) per write so a stalled peer can't hang the goroutine indefinitely; `startTicker` runs the single ticker goroutine for the server's lifetime, gated on `registry.count() == 0` so a zero-subscriber tick performs no filesystem work at all, calling `store.ensureFresh()` and broadcasting only when that call actually rebuilt
  - `rail_handler.go` — `NewRailHandler` for `GET /rail/<relpath>`; renders four htmx OOB fragments: `#rail-props-content` (YAML frontmatter key/values in source order via `frontmatter.ParseOrdered`; scalars pass through as plain text, non-primitive values (`[]any` arrays and nested `map[string]any` objects) are pretty-printed as JSON via `json.MarshalIndent` and rendered in a `<pre><code>` block flagged by `propKV.IsJSON bool` — replacing the unreadable Go `fmt` default for these types; `propKV` also has an `IsURL bool` field — when true the template renders the value as `<a href="..." target="_blank" rel="noopener">`; `isHTTPURL(s)` is a prefix-check helper for `http://` / `https://`; slot CSS-hidden when empty), `#rail-out-content` (outbound links with broken/codefile/ambiguous/external annotations), `#rail-in-content` (backlinks + orphan note), `#rail-graph-content` (Cytoscape mini-graph seeded by `/graph/data?node=<page>&depth=1`); `cyID` is derived from the relpath with slashes/dots replaced by hyphens for valid HTML id; uses `data-rail-graph-url` attribute + `htmx.onLoad` pattern (inline `<script>` in OOB swaps unreliable in htmx); path-traversal guard + graph-membership O(1) check via `g.Has(rel)` both enforced before reading the file; `NewRailHandler`'s `g graphProvider` parameter is the same seam `context_handler.go` uses — a static `*Graph` in tests, or the shared `*snapshotStore` in production (live-reload) — resolved once per request via `g.currentGraph()` so the rail reflects live realm state; `isNilGraphProvider(g)` degrades every request to 404 when there is no graph to confirm membership against
  - `search_md.go` — `NewMdSearchHandler` for `GET /search/md?q=`; literal case-insensitive substring grep across all `.md` files under `NavRoot`; cap=50 results, snippet truncated to 120 chars; empty query returns empty fragment (200); each result carries `/page/<relpath>` navigation hook
  - `graphoverlay.go` — `/graph/data` Cytoscape-shaped elements JSON (CP9, SC11) — the docs graph's data contract, unchanged in shape even though the docs graph view itself now renders via cosmos.gl rather than Cytoscape; `buildCytoElements` and `buildLocalSubgraph` call `g.NodeType(n)` on each node and pass the result as `Type` in `cytoNodeData`; the `/graph/data` JSON carries `type` on each node element — consumed by the rail mini-graph's Cytoscape selectors (`atomicCyStyle()`) AND by `system-graph.js`'s cosmos.gl adapter (via the shared `atomicCyTypeColors()` color source, see `templates/layout.html`) for the URL-addressable `/graph` page's docs view, which carries a type legend/filter; `NewGraphDataHandlerWithGraph(root, store)` (the production constructor; `store` must not be nil) sources the full-view graph through the shared snapshot store (`store.currentGraph()`) rather than a startup-frozen graph, and the full-view response itself is served from `graphcache.go`'s `graphDataCache` — a fingerprint-keyed, singleflight-deduped cache warmed at startup that re-assembles (`BuildProvenanceDAG` + `buildCytoElements` + provenance-edge injection) only when the store's fingerprint has changed and otherwise returns cached bytes verbatim; the fingerprint is surfaced via the `X-Graph-Fingerprint` header so the browser's own layout cache keys off exact realm state
  - `code_members.go` — `memberResolver` struct (`realmRoot`/`claudeMDPath`/`wikiIndexPath` fields, `members()`/`localDBPath()` methods) embedded by `codeExplorerHandler`, `codeGraphHandler`, and `codeGraphMembersHandler` so code-member discovery — federation (`<code-index>` config + per-member dbs) unioned with per-member self-indexes ([`.claude/.atomic-index/atomic.db`](../../.claude/.atomic-index/atomic.db), discovered via the wiki scan) — and the local-db fallback path live in exactly one place instead of being duplicated per handler; `discoverCodeMembers`, `realmCodeMembers`, `memberDB`, `memberForPath`, `joinMemberPath`, `fileExists` helpers
  - `codesearch.go` — federated code search (`/code/search`); fans out via `engine.NewWithDBPath` per member
  - `codeexplorer.go` — per-repo Code Explorer routes under `/code/*`: `node`, `callers`, `callees`, `impact`, `files`, `schema`; plus `/code/file?path=<relpath>` (FE-new) which lists all symbols in a source file using `GetNodesInFile`; `renderNodeDetail`'s root element always stamps `data-name`, and additionally stamps `data-file` (member-aware, via `joinMemberPath`) plus `data-line` when the node has a location — `templates/layout.html`'s `htmx:after:swap` handler on `#code-modal-intel` reads these to mirror the modal's source pane, line scroll, and title onto whatever `/code/node` view was just swapped in; list views (`renderSubgraph`'s callers/callees/impact edge chips, file-defines) carry no `data-file` so the source pane is left untouched for those; test coverage: `TestCodeExplorer_NodeDetail_StampsSourceAttrs`, `TestCodeExplorer_Callers_NoSourceAttrs` (`codeexplorer_test.go`)
  - `codegraph.go` — `NewCodeGraphHandler` for `GET /code/graph/data[?member=<prefix>]`; serves the resolved member's entire symbol graph (all nodes + all edges) as flat JSON (`graphDataResponse{Fingerprint, Nodes, Edges}`) for the code graph view; an explicit `?member=` that fails to resolve returns a 404 JSON error body (`writeGraphError`), unlike the sibling HTML `/code/*` explorer routes which fall back silently to the local index; `graphFingerprint` derives a content-sensitive SHA-256 from sorted `(id, label, line)` node tuples and sorted `(source, target, kind)` edge tuples — not counts + a timestamp — so a renamed-symbol layout cache never replays stale positions; test coverage: `codegraph_test.go`
  - `code_graph_members.go` — `NewCodeGraphMembersHandler` for `GET /code/graph/members`; lists the served scope's code members with each one's indexed state via a cheap `fileExists` check on the member's db path (no engine open); repo scope always returns exactly one member with an empty prefix — the FE only renders the realm member picker when more than one member comes back; test coverage: `code_graph_members_test.go`
  - `external.go` — external-link registry (`/external`)
  - `health.go` — realm health page handler (now mounted at `/status`)
  - `provenance.go` — provenance DAG (`/provenance`); reads `reflects:` / `sources:` frontmatter; uses `wiki.ResolveFingerprint`
  - `stale.go` — `parseStaleLines`; shared `wiki.Stale` output parser used by both `computeStaleness` (nav.go) and `productionWikiStale` (health.go); parses `DRIFT` and `STALE` lines into `staleSets` (Members, Buckets, Concerns); unknown lines silently skipped
  - `walk.go` — `shouldSkipDir` and `hiddenFile`; [`.claude`](../..) is NOT skipped (holds servable project docs); other dotdirs, `node_modules`, `vendor`, `tmp` are skipped; nested dotdirs inside [`.claude`](../..) (e.g. `.scratchpad`, `.atomic-index`) are still skipped by the leading-dot rule
  - `context_handler.go` — production page handler `NewPageHandlerWithGraph`; calls `RenderMarkdownWithGraph` (not `RenderMarkdownWithLinks`) so all page renders receive in-body wikilink resolution when a `*Graph` is available; handles directory URLs by probing `resolveDirIndex` then falling back to `directoryListingHTML`; `breadcrumbSegments` wraps ancestor path segments in `<a data-nav-folder=…>` for client-side nav expansion; `serve404` renders shell-wrapped 404 with status 404 and `LandingURL` pointing at the missing path so the htmx pipeline delivers the in-shell "not found" fragment; `NewPageHandlerWithGraph`'s `g graphProvider` parameter accepts either a static `*Graph` (tests, pre-live-reload callers) or the shared `*snapshotStore` (production, live-reload) — `isNilGraphProvider` degrades a typed-nil of either kind (or any nilable-kind implementor) to `NewPageHandler` with no rail wiring, since a plain `g == nil` check is always false for a boxed typed-nil interface value; the handler calls `g.currentGraph()` exactly once per request so one render sees one consistent graph even if a live rebuild lands mid-request
  - `assets/app.css` — base CSS (embedded); nav-item links styled amber (matching right-rail OUT/IN links) with 20px left inset so leaves read as inside their section; visited/hover/active states pinned explicitly to amber/amber-bright; brand `text-decoration: none` + hover color lock; includes `.md-content a.wikilink`, `.md-content a.wikilink-ambiguous`, `.md-content .wikilink-broken` rules; ~50 lines added for graph-pane controls (`.graph-view-switch` Docs|Code switcher, member picker)
  - [`assets/logo.png`](../../assets/logo.png) — brand logo (embedded via `go:embed assets templates`); rendered in top-bar brand link
  - `assets/vendor/` — vendored JS: `htmx.min.js`, `mermaid.min.js`, `cytoscape.min.js` (rail mini-graph only), `cosmos-graph.js` (GPU-simulated graph pane; `cosmos-graph.provenance.txt` alongside it records the exact package version, dependency pins, and bundling command) (embedded via `go:embed assets templates`)
  - `templates/layout.html` — single Obsidian-style HTML shell (top bar · left nav · middle pane with page/system toggle · right rail); `atomicCyTypeColors()` is the single-source type→color map (reads CSS custom properties so colors re-theme on light/dark toggle); `atomicCyStyle()` builds Cytoscape style selectors from it for the right-rail mini-graph ONLY — per the source comment, the docs/code graph views moved to cosmos.gl (CP1/CP2) and have no Cytoscape style objects, consuming `atomicCyTypeColors()` directly via JS instead; type legend/filter present on the docs graph view; brand `<span>` replaced with `<a href={{.LandingURL}}>` (htmx-navigable home link); `btn-graph` click handler migrated to delegated `document.addEventListener('click', …)` so it survives htmx history-restore body swaps; search JS refactored: `window.__atomicSearchBooted` guard prevents listener stacking on body swaps, all element lookups via `byId()` helper at use time, all click listeners delegated on `document`; `htmx:after:swap` listener resets `#main-pane.scrollTop = 0` after content swaps (history restores swap `<body>`, not `#main-pane`, so Back/Forward restore their position); modal `scrollTop = 0` on open; script tag order is `cosmos-graph.js` (vendored, exposes `window.Cosmos`) → `graph-core.js` (view-agnostic engine, must load before any per-view profile) → `system-graph.js` (docs profile) → `code-graph.js` (code profile); the FE3 `htmx.onLoad` mount delegation calls `SystemGraph.enterGraphMode()` (reads the `/graph` URL's `view`/`member` params and renders the matching profile) instead of a bare `SystemGraph.mount(c)`; `openCodeNode(id, member, meta)` opens the existing code-explorer node modal for one symbol (the code graph's click hook) and publishes itself as `window.AtomicCodeExplorer = { openNode: openCodeNode }`, mirroring the `window.AtomicGraphUI` pattern the docs profile uses for its preview-card/modal hooks; gained a Docs|Code switcher + realm member picker in the graph pane; the code-modal script's intel-pane back-stack forward-push moved from a document-level `click` listener (walking up to an `A[hx-get]` inside `#code-modal-intel` — never fires under htmx 4's own delegated click handling) to the existing `htmx:before:request` handler, which reads the request URL off `ctx.request.action`; a new `htmx:after:swap` handler on `#code-modal-intel` reads `data-file`/`data-line`/`data-name` off the swapped-in view's root element (stamped by `renderNodeDetail`, `codeexplorer.go`) and mirrors them onto `#code-modal-source`/`#code-modal-title`; `loadSource(filePath, onLoaded)` is the single fetch path for `#code-modal-source` and tracks the currently-loaded path in `currentSourcePath`, skipping the fetch (and loading-flash) when navigating between symbols in the same file — `currentSourcePath` is reset to `null` when a swapped-in view carries no location so the next `loadSource` call does not wrongly skip its fetch; live-reload (CP4): a boot-guarded inline script (`window.__atomicEventsBooted`, same idiom as the search-dialog guard, so a history-restore body swap doesn't re-connect) opens one plain `EventSource('/events')` — not the htmx SSE extension, not WebSockets — whose `onmessage` JSON-parses each `{fp, changed}` message, treats the always-first resync message as fp-seeding only (`lastSeenFp` starts `null`), and otherwise dispatches an `atomic:live-reload` DOM `CustomEvent` (the documented attachment point for a future graph-pane reconciler — see Conventions) consumed by `pageModeReconcile`: it always refetches `#nav-pane` (`/nav?live=1`), and refetches `#main-pane` (`/page/<rel>`) only when the page currently displayed (`#page-content[data-relpath]`) is in the event's `changed` list, or the list was omitted because the diff exceeded its cap; both refetches go through a `liveSwap()` helper that tags the request with an `HX-Live-Swap` header so the shared `htmx:after:swap` scroll-reset listener (which otherwise zeroes `#main-pane.scrollTop` on every content swap) recognizes and skips a live-triggered swap, preserving the reader's scroll position; a `#conn-indicator` element (live / reconnecting / disconnected) is painted from `es.onopen`/`es.onerror` plus `es.readyState`, and repainted on every `htmx:after:swap` since a full-body history-restore swap replaces the indicator's DOM node while the underlying `EventSource` connection survives untouched
- [`atomic/cmd/atomic/main.go`](../../atomic/cmd/atomic/main.go) — dispatches `atomic serve` at line 131–132 via `serve.Run(args[1:], ...)`
- [`atomic/internal/cliusage/cliusage.go`](../../atomic/internal/cliusage/cliusage.go) — `serve` verb entry: path `["serve"]`, args `[path]`, flags `["--port", "--open"]`
- [`atomic/internal/serve/assets/graph-core.js`](../../atomic/internal/serve/assets/graph-core.js) (NEW) — view-agnostic cosmos.gl engine extracted from `system-graph.js` ([`docs/spec/code-graph.md`](../spec/code-graph.md) checkpoint 4); owns mount/teardown/retheme lifecycle, WebGL2 detect-and-message, the tuned motion policy (settle-then-pause constants, drag reheat with a repulsion boost, styling-flush ordering), the IndexedDB layout cache, the DOM label overlay (`computeLabelSet()`, moved here from `system-graph.js` — exported for [`scripts/test-system-graph-culling.cjs`](../../scripts/test-system-graph-culling.cjs)), the legend chip machinery, drag handling, and the `debugState()`/`simRunning()` test accessors consumed by [`scripts/graph-gates.mjs`](../../scripts/graph-gates.mjs); delegates to a per-view `PROFILE` object passed to `mount(container, profile)` (data fetch + adapter, palette/taxonomy, cache key, label/meta resolvers, hover/click hooks, optional `onTeardown()`); `window.GraphCore`
- [`atomic/internal/serve/assets/code-graph.js`](../../atomic/internal/serve/assets/code-graph.js) (NEW, ~275 lines) — the "Code Graph" view profile wired to `graph-core.js`; fetches `/code/graph/data[?member=]`, caches by `code:<member>:<fingerprint>` (namespaced so it never collides with the docs profile's own IndexedDB layout-cache entries, which key on the SAME store), adapts the flat JSON to cosmos, groups the 38 codeintel `NodeKind` values into ~8 visual kind-groups (`KIND_GROUPS` → `kindToGroup`, unrecognized kinds default to `'other'`) with a filterable legend, styles `contains` edges fainter than `calls`/`imports`, and routes node clicks through `window.AtomicCodeExplorer.openNode`; `window.CodeGraph`
- [`atomic/internal/serve/assets/system-graph.js`](../../atomic/internal/serve/assets/system-graph.js) — slimmed drastically (now the "docs" profile consuming `graph-core.js`): owns the `/graph/data` fetch+adapter, the OKF type→color palette, provenance/drift edge styling, and the docs-shell navigation glue (`#btn-graph` toggle, current-page tracking); `enterGraphMode()` reads the `/graph` URL's `view`/`member` params and calls `renderGraphPane`, which rebuilds `#main-pane` and mounts either `window.SystemGraph` (docs) or, via `mountCodeView`, `window.CodeGraph` (code) after one `/code/graph/members` fetch to resolve/default the member and fill the picker; `window.SystemGraph`
- [`scripts/graph-gates.mjs`](../../scripts/graph-gates.mjs) (NEW, ~460 lines) — a LOCAL (not CI) Playwright gate harness: drives headless Chromium against a running `atomic serve` (via `--url` or spawned via `--serve-bin`) and checks 5 behavioral gates (clean mount, settle-then-pause, drag/overlap resolution, IndexedDB cache-replay with zero motion, hover preview) for `--view docs` or `--view code`; exits non-zero on any gate failure; `--view code` drives the real UI path (lands on the page, clicks `#btn-graph`, then the Docs|Code switcher's Code button) rather than navigating directly; run by hand after touching the graph assets or `templates/layout.html` — this repo's CI has no browser/GPU step; `playwright` is pinned exact (`1.61.1`, no `^`) as a root [`package.json`](../../package.json)/[`package-lock.json`](../../package-lock.json) devDependency
- [`scripts/test-system-graph-culling.cjs`](../../scripts/test-system-graph-culling.cjs) — manual-run unit test for `computeLabelSet()`, updated to load it from its new home in `graph-core.js` instead of `system-graph.js`

## Docs

- [`docs/spec/atomic-serve.md`](../../docs/spec/atomic-serve.md) — success criteria, non-goals, security contract; SC11 amended to describe graph mode hosting Docs + Code views behind a nested control, the shared cosmos.gl core, member-picker behavior, and the code-view click→code-explorer-modal behavior
- [`docs/design/atomic-serve.md`](../../docs/design/atomic-serve.md) — design decisions: scope model, asset vendoring, route list, Cytoscape+ELK choice
- [`docs/reference/serve.md`](../../docs/reference/serve.md) — user-facing reference for all flags, scope resolution table, every route; "Graph mode" section documents the Docs|Code nested toggle, including the Code graph view (kind-grouped legend, `contains`-edge de-emphasis, node-size-by-degree, member picker, per-repo fingerprint-keyed layout cache, "index not available" message)
- [`docs/design/cosmos-system-graph.md`](../design/cosmos-system-graph.md) — design doc for the docs-graph engine swap (Cytoscape canvas 2D + one-shot cola layout → cosmos.gl GPU simulation): problem statement (no continuous physics, no headroom past ~3k nodes), goals/non-goals (rail mini-graph and `/graph/data` contract explicitly out of scope; symbol/code-graph rendering explicitly deferred as future headroom, later built by [`docs/design/code-graph.md`](../design/code-graph.md)/[`docs/spec/code-graph.md`](../spec/code-graph.md))
- [`docs/spec/cosmos-system-graph.md`](../spec/cosmos-system-graph.md) — implementation spec for the docs-graph engine swap; the rendering contract [`docs/spec/atomic-serve.md`](../spec/atomic-serve.md)'s SC11 and [`docs/spec/code-graph.md`](../spec/code-graph.md) both delegate to
- [`docs/design/code-graph.md`](../design/code-graph.md) (NEW) — design deliberation for the code graph view: granularity (symbol-level full graph chosen over file-level aggregation or a two-level drill), JS reuse shape (shared cosmos core + per-view profiles chosen over forking `system-graph.js` or in-place branching), data endpoint shape (lean flat JSON chosen over reusing `/graph/data`'s Cytoscape-envelope shape)
- [`docs/spec/code-graph.md`](../spec/code-graph.md) (NEW) — implementation spec + checkpoints for the code graph view: CP1 index-integrity guard, CP2 `/code/graph/data` export endpoint + fingerprint, CP3 committed gate harness, CP4 shared cosmos core extraction, CP5 code view profile (kind→group palette, edge-kind styling, layout cache), CP6 Docs|Code switcher + member picker, CP7 docs amendments
- [`docs/design/serve-live-reload.md`](../design/serve-live-reload.md) — design deliberation for live-reload: problem statement (nav walks per request, the Network View cache stat-walked its own fingerprint per request, and the link graph was built once at startup and frozen — three independent freshness models); two distinct goals kept explicit (fixing the frozen-graph staleness bug vs. the separate live-watch UX requirement); goals/non-goals (one walk mechanism, live-updating open views, zero server work with no browser tab open, no fsnotify, no WebSockets, whole-realm fingerprint as the unit of change, no user-facing tick-interval flag)
- [`docs/spec/serve-live-reload.md`](../spec/serve-live-reload.md) — implementation spec, checkpoints CP1–CP5; CP5 ("system-mode client patching") is struck through in the current body and superseded per the 2026-07-08 change-log entry — its Cytoscape-specific implementation (commits `5c6dbc3`, `78d75be`) had no surviving attachment point once PR #123's cosmos.gl engine swap replaced the docs-graph client wholesale; server-side flows (CP1–CP4: snapshot store, `/events`, page-mode reconcile) are untouched and remain current; successor work is tracked by follow-up `cosmos-graph-live-reload-reconcile`

## Coupling

- **wiki domain**: `serve` calls `wiki.ReadBucketEntries`, `wiki.ReadScanMembers`, `wiki.Stale`, `wiki.CheckStaleness`, `wiki.ResolveFingerprint`, and `wiki.FileSHA256`. Changes to wiki's staleness or bucket-diff APIs break serve's health page and nav badges.
- **code-intel domain**: `serve` imports `codeintel/realm` (for `realm.Resolve`) and opens per-member indexes via `engine.NewWithDBPath`. Changes to `realm.Resolve` scope types or `engine.NewWithDBPath` signature require matching changes in `serve.go` and `codesearch.go`. `codeexplorer.go` uses `GetNodesInFile` from `CodeEngine`; changes to that method signature propagate here. `CodeEngine` also declares `GetAllNodes(ctx)` and `GetAllEdges(ctx)` — pass-throughs to the engine facade's bulk DB reads at [`atomic/internal/codeintel/engine/engine.go`](../../atomic/internal/codeintel/engine/engine.go), added specifically to back `codegraph.go`'s `/code/graph/data` full-graph export; changes to either method's signature propagate to `codegraph.go`. `CodeEngine`'s `GetCallers`/`GetCallees`/`GetImpactRadius` are backed by [`atomic/internal/codeintel/graph/`](../../atomic/internal/codeintel/graph)'s `Manager` — `codeexplorer.go`'s `renderSubgraph` depends on `GetImpactRadius`'s container-descend children resolving in `Subgraph.Nodes` (its own root-substitution already tolerated the pre-fix hollow `GetCallers`/`GetCallees` start-node entry, so that half of the hydration fix serves direct CLI/MCP/`codectx` consumers of the `Subgraph` contract rather than `renderSubgraph` itself). `GetCallers`/`GetCallees` now also error (rather than returning an empty subgraph) for an unresolvable node ID.
- **mdlink domain**: `serve` depends on `mdlink.ExtractLinks` for graph and backlink construction. `Edge.CodeFile` is populated from `mdlink.Link` fields; changes to `Link` struct or wikilink resolution rules affect `graph.go`, `rail_handler.go`, and `graphoverlay.go`. `wikilink.go` imports `mdlink` for the `mdlink.Wikilink` edge-kind constant used to filter outbound edges in `wikilinkResolverFromGraph`; a rename of that constant breaks the resolver.
- **cliusage / doctor domain**: `atomic validate artifacts` lints `atomic serve` citations against `cliusage.go`. Adding or removing serve flags requires updating `cliusage.go` or artifact linting false-positives.
- **frontmatter package** ([`atomic/internal/frontmatter/`](../../atomic/internal/frontmatter)): `rail_handler.go` calls `frontmatter.ParseOrdered` for the Properties slot; `render.go` calls `frontmatter.Parse` to strip frontmatter before goldmark. Changes to `KV` type, `ParseOrdered` signature, or `splitFrontmatter` semantics propagate to both handlers.

## Conventions worth knowing

- `Run` / `RunWithContext` split: `Run` owns signal handling and calls `os.Exit`; `RunWithContext` is the testable seam — tests inject a context and `Options` directly.
- All static assets (CSS, vendored JS, HTML template) are embedded at compile time via `//go:embed assets templates`; no network fetch and no file dependency outside the binary at runtime.
- Path traversal is rejected at the handler level — every served path is resolved against the scope root and 404'd if it escapes; `safeResolve` is the shared guard used by page, rail, and file handlers.
- `parseFlags` normalizes the positional target-dir arg to an absolute path via `filepath.Abs` so downstream handlers can resolve request paths against the root regardless of invocation form (`atomic serve .` or a relative path).
- cosmos.gl script load order is fixed: `cosmos-graph.js` (vendored, exposes `window.Cosmos`) → `graph-core.js` (view-agnostic engine; must load before any per-view profile) → `system-graph.js` (docs profile) → `code-graph.js` (code profile). The rail mini-graph's `cytoscape.min.js` is a separate, unrelated load — the rail never uses cosmos.gl.
- `graph-core.js` / profile split: `graph-core.js` owns mount/teardown/retheme lifecycle, WebGL2 detection, the tuned motion policy, the IndexedDB layout cache, the DOM label overlay, the legend chip machinery, and drag handling; a profile (passed to `window.GraphCore.mount(container, profile)`) supplies only the data fetch + response adapter, palette/taxonomy, cache key, label/meta resolvers, hover/click hooks, and an optional `onTeardown()` hook. `system-graph.js` (docs, over `/graph/data`) and `code-graph.js` (code, over `/code/graph/data`) are the two current profiles; both run the core's drag-physics constants verbatim — a per-profile override was removed as unused after a CP5 empirical sweep found the constants were never the bottleneck for a forced drag overlap at code-graph scale (the gate's own drag-target node pick was — see [`scripts/graph-gates.mjs`](../../scripts/graph-gates.mjs)'s gate 3).
- Fingerprint-as-cache-key: both graph endpoints derive a content-sensitive fingerprint (sorted node/edge tuples, not counts + a timestamp) so a renamed-symbol layout cache never replays stale positions. The code view's cache key is namespaced `code:<member>:<fingerprint>` — a bare fingerprint would collide with the docs profile's own entries, since both profiles share the SAME IndexedDB store (`LAYOUT_DB = 'atomic-serve'`, `LAYOUT_STORE = 'graph-layout'`, `CACHE_FORMAT_VERSION = 2`, all defined in `graph-core.js`).
- Kind→group taxonomy: `code-graph.js`'s `KIND_GROUPS` table maps the 38 codeintel `NodeKind` values (module-file, type, callable, value, sql-data, sql-routine, import-export, other — ~8 visual groups) with a legend that filters groups client-side; a kind absent from the table (a future `types.go` addition, or an unexpected server value) falls through to the `'other'` default bucket via `kindToGroup`, so every node always resolves to a defined, colored group.
- `memberResolver` (`code_members.go`) is the single seam for code-member discovery — federation ∪ self-index union, plus the local-db fallback path — embedded by `codeExplorerHandler`, `codeGraphHandler`, and `codeGraphMembersHandler` rather than duplicated per handler.
- [`scripts/graph-gates.mjs`](../../scripts/graph-gates.mjs) and [`scripts/test-system-graph-culling.cjs`](../../scripts/test-system-graph-culling.cjs) are LOCAL developer tools, not CI gates — this repo's CI has no browser/GPU step, so both are run by hand after touching the graph assets or `templates/layout.html`. `graph-gates.mjs`'s drag-overlap gate deliberately picks mid-size nodes as the drag-target pair rather than the first two nodes in insertion order.
- `--port 0` triggers OS-assigned port; the chosen port is printed to stdout so callers (tests, scripts) can parse it.
- `DisplayScopeRepo` covers both "repo with a code index" and "bare repo with no index" — docs-only mode is valid; a code index is not required to start.
- `GET /rail/<relpath>` emits four OOB fragments (not three): `#rail-props-content` (YAML frontmatter properties), `#rail-out-content`, `#rail-in-content`, `#rail-graph-content`. The properties slot is hidden via CSS when the page has no frontmatter. HTTP(S) URL property values render as clickable `<a>` anchors (target=_blank) via the `propKV.IsURL` field.
- Right-rail mini-graph uses `data-rail-graph-url` attribute + `htmx.onLoad` delegation for Cytoscape init; inline `<script>` tags in OOB `innerHTML` swaps are not reliably executed by htmx. `data-rail-graph-url` is intentionally distinct from the `[system]` toggle's click-handler attribute to avoid selector collision.
- `/healthz` is the liveness probe (plain text `ok`); `/status` is the user-facing realm-health dashboard (moved from `/health` in CP6).
- In production the link graph is served through the shared `*snapshotStore` (`serve.go`'s `NewSnapshotStore(navRoot)`), not a startup-frozen `*Graph` — `ensureFresh()` rebuilds it via `BuildLinkGraph` whenever the realm's fingerprint changes (see the live-reload conventions below). Page and rail handlers read it through the `graphProvider` interface (`g.currentGraph()`) so they remain stateless either way; a bare `*Graph` built once via `BuildLinkGraph` directly is still a valid `graphProvider` for tests and other static callers. `Graph.Has(rel)` is O(1) via `nodeSet`.
- Wikilink resolution is single-source: `wikilinkResolverFromGraph` reads the already-computed outbound edges from `Graph.Outbound(pageRelPath)` filtered to `mdlink.Wikilink` kind. The body and the right rail use the same resolution — no second resolution pass. When `g` is nil (e.g. tests calling `RenderMarkdown` or `RenderMarkdownWithLinks`), the wikilink parser is not registered and `[[…]]` renders as literal text.
- The `wikilinkInlineParser` priority is 150, below the goldmark default block parsers but above the default link parser (200). On a single `[` that does not form `[[`, it returns nil and does not advance the reader — the default link parser takes over.
- Bundle-root-relative links (leading-slash form, e.g. `/repos/foo.md`) are resolved identically at graph-build time (`resolveMarkdownLink`) and at render time (`resolvePageHref`) via the shared `resolveRootRelative` helper; the two functions cannot diverge on this link form.
- Node type classification is cached in `Graph.nodeTypes` at build time (one pass, no second file read); `NodeType(relPath)` is O(1) at render time. The `type` field on each `/graph/data` node element is colored via the shared `atomicCyTypeColors()` map (`templates/layout.html`) — Cytoscape selectors (`atomicCyStyle()`) consume it for the right-rail mini-graph only; the docs graph view consumes the same map directly via `system-graph.js`'s cosmos.gl adapter, not Cytoscape.
- htmx 4's delegated click handling consumes clicks on `A[hx-get]` elements before a document-level `click` listener ever sees them — the code-modal's intel-pane back-stack push (`templates/layout.html`) never fired on a `click` listener for this reason, and now happens on the `htmx:before:request` event instead (reads the pushed URL off `ctx.request.action`).
- `graph.Manager`'s `GetImpactRadius` (see code-intel domain) hydrates each container-descend child into the returned `Subgraph.Nodes` — `renderSubgraph` (`codeexplorer.go`) relies on that hydration to render a name/kind/location chip for container children instead of falling back to the raw node ID; `GetCallers`/`GetCallees`'s own start-node hydration is a `Subgraph`-contract fix for direct CLI/MCP/`codectx` consumers, since `renderSubgraph`'s root-substitution already tolerated the pre-fix hollow entry.
- Live-reload's ticker is a single goroutine, started once at server startup and stopped by the same context that drives graceful shutdown — never started or stopped on individual subscribe/unsubscribe edges. It is gated on subscriber count (`registry.count() == 0` skips even the cheap stat-only fingerprint walk), so an idle `atomic serve` with no browser tab open costs nothing beyond the ticker's own timer wakeups.
- The realm snapshot (`snapshot.go`) is published via a single `atomic.Pointer[realmSnapshot]` swap, never mutated field-by-field — a reader that loads the pointer once always sees an internally consistent `{fp, navPaths, graph, manifest}` struct. A torn read (a fresh fingerprint paired with a stale graph) is structurally impossible.
- The snapshot's cheap validation is a quiet-window-filtered, stat-only fingerprint walk (`fingerprint()`, ~2s window, no file content read): a file whose mtime falls within the last 2 seconds is excluded from the manifest so a file mid-write can't flip the fingerprint out from under a reader. Only when the fingerprint changes does the expensive `BuildLinkGraph` content-reading rebuild run.
- The `/events` `changeEvent.Changed` manifest-diff list is capped at 100 entries (`changedCap`); above the cap the field is omitted (`omitempty`) rather than truncated. The client (`templates/layout.html`) treats an omitted `Changed` the same as an oversized one — refetch everything rather than trust a partial list.
- **Live-reload does not patch the graph pane in place.** Checkpoint 5 of [`docs/spec/serve-live-reload.md`](../spec/serve-live-reload.md) planned an in-place Cytoscape id-diff/patch for the graph pane, built against the pre-cosmos `system-graph.js` client (`mountSystemGraph`, `window.__systemCy`); PR #123's cosmos.gl engine swap replaced that client wholesale before CP5 could be re-attached, so CP5's implementation (commits `5c6dbc3`, `78d75be`) was superseded and dropped at the merge — see the spec's 2026-07-08 change-log entry. Today, `templates/layout.html`'s live-reload script still dispatches an `atomic:live-reload` DOM event while the graph pane is mounted, but nothing listens for it there — the graph pane only reflects a realm change on its next `/graph/data` (or `/code/graph/data`) fetch (re-entering the view, reloading, or the cosmos engine's own fingerprint-keyed layout cache invalidating on a subsequent load). Tracked by follow-up `cosmos-graph-live-reload-reconcile`.
