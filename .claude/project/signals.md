# Project signals

## Framework & runtime

- **Go 1.25**, single module at [`atomic/go.mod`](../../atomic/go.mod) (`module github.com/damusix/atomic-claude/atomic`). CLI binary named [`atomic`](../../atomic).
- **codeintel tsbinding sub-module**: [`atomic/internal/codeintel/tsbinding/go.mod`](../../atomic/internal/codeintel/tsbinding/go.mod) (`module github.com/malivvan/tree-sitter`, go 1.23.4) — wazero + tree-sitter WASM binding, separate module.
- **External Go deps**: `gopkg.in/yaml.v3` (YAML frontmatter), `github.com/tailscale/hujson` (lenient JSON for hooks/settings), `github.com/pelletier/go-toml/v2` (TOML config), `github.com/charmbracelet/huh` (interactive TUI prompts), `github.com/tetratelabs/wazero` (WASM runtime for tree-sitter), `github.com/modelcontextprotocol/go-sdk` (MCP server).
- **Go embed**: `//go:embed all:bundle` in [`atomic/internal/embedded/bundle.go`](../../atomic/internal/embedded/bundle.go). `all:` prefix required to include [`commands/_templates/`](../../commands/_templates) (underscore prefix skipped by plain `bundle` glob).
- **goreleaser**: multi-platform release — `linux/darwin x amd64/arm64`. CGO disabled. Version from `internal/version` via ldflags.
- **release-please**: automated changelog + tag on main push.
- **VitePress docs site**: [`package.json`](../../package.json) (`name=atomic-claude-docs`), [`package-lock.json`](../../package-lock.json), [`.vitepress/config.mts`](../../.vitepress/config.mts). Scripts: `docs:build`, `docs:dev`, `docs:preview`. Deployed via [`.github/workflows/docs.yml`](../../.github/workflows/docs.yml). Not part of the Go build.
- No web framework. SQLite used only by the code-intel engine at `<projectRoot>/.claude/.atomic-index/atomic.db`.

## Build / test / lint

All commands run from [`atomic/`](../../atomic) (CI: `working-directory: ./atomic`):

| Purpose | Command | Source |
|---------|---------|--------|
| Render command templates | `make render` (root) or `make -C atomic render` | [`atomic/Makefile`](../../atomic/Makefile), CI "Verify render is committed" |
| Regenerate embedded bundle | `go generate ./...` → `make -C atomic bundle` | [`atomic/Makefile`](../../atomic/Makefile) target `bundle`, CI "Verify bundle is committed" |
| Run tests | `go test ./...` | [`atomic/Makefile`](../../atomic/Makefile) target `test` |
| Vet | `go vet ./...` | [`atomic/Makefile`](../../atomic/Makefile) target `vet` |
| Format check | `gofmt -l .` | [`atomic/Makefile`](../../atomic/Makefile) target `fmt` |
| Build binary | `go build -o ../bin/atomic ./cmd/atomic` | [`atomic/Makefile`](../../atomic/Makefile) target `build` |
| Tidy deps | `go mod tidy` | [`atomic/Makefile`](../../atomic/Makefile) target `tidy` |
| Release | `goreleaser release --clean` | [`.github/workflows/release.yml`](../../.github/workflows/release.yml) (on `v*` tag) |

CI gates: (1) `make render && git diff --exit-code` — stale [`commands/`](../../commands) fails. (2) `make bundle && git diff --exit-code` — stale `manifest.go` fails. Pipeline order: render must run before bundle.

## Language breakdown

| Language | LOC | Files | % |
|----------|-----|-------|---|
| Go | 134705 | 390 | 69% |
| Markdown | 47283 | 283 | 24% |
| JavaScript | 4179 | 5 | 2% |
| CSS | 2943 | 2 | 1% |
| JSON | 2363 | 6 | 1% |
| HTML | 1653 | 1 | 0% |
| Shell | 910 | 8 | 0% |
| YAML | 210 | 6 | 0% |
| TypeScript | 205 | 3 | 0% |
| Vue | 183 | 1 | 0% |

Note: C sources (`tsbinding/src/`) are vendored-on-demand and no longer committed. Go LOC decreased 134834 → 134705 (390 files) from validate/config.go C1 rule removal (92 lines removed). Markdown LOC increased 47253 → 47283 (283 files, down from 285) after spec updates; 2 files net removed. JavaScript LOC dropped significantly (10034 → 4179) after htmx upgrade to 4.0.0-beta4 (smaller bundle). CSS LOC increased (2015 → 2943) reflecting FE polish additions to [`atomic/internal/serve/assets/app.css`](../../atomic/internal/serve/assets/app.css) and VitePress theme updates. HTML LOC increased (899 → 1653) from layout.html additions.

## DevOps & CI

- CI: GitHub Actions ([`.github/workflows/`](../../.github/workflows)). Four workflows: `ci.yml` (test/vet/fmt/bundle-drift gates), `docs.yml` (VitePress site build/deploy), `release-please.yml` (changelog automation), `release.yml` (goreleaser on tag).
- Release: goreleaser produces multi-arch binaries. release-please manages version + tag.
- Pre-commit hook ([`.githooks/pre-commit`](../../.githooks/pre-commit)): three-stage render→bundle→followups chain. Install via `make hooks`.

## Domains

Each domain groups ALL files across ALL layers (artifacts + CLI code + docs) for one feature concern. Read a domain file when you're working on that feature end-to-end.

| Domain | Repo paths | One-liner | Detail |
|--------|------------|-----------|--------|
| signals | [`agents/atomic-signals-inferrer.md`](../../agents/atomic-signals-inferrer.md), [`commands/refresh-signals.md`](../../commands/refresh-signals.md), [`atomic/internal/signals/`](../../atomic/internal/signals), [`atomic/internal/doctor/checks_signals.go`](../../atomic/internal/doctor/checks_signals.go), [`atomic/internal/doctor/checks_refs.go`](../../atomic/internal/doctor/checks_refs.go), `docs/spec/signals-*.md` | Scan → infer → wire: project context generation pipeline | [`.claude/project/signals/signals.md`](signals/signals.md) |
| bundle | [`templates/commands/`](../../templates/commands), [`templates/agents/`](../../templates/agents), [`templates/shared/`](../../templates/shared), [`commands/`](../../commands), [`agents/`](../../agents), [`skills/`](../../skills), [`output-styles/`](../../output-styles), [`rules/`](../../rules), [`CLAUDE.md`](../../CLAUDE.md), [`atomic/internal/bundlespec/`](../../atomic/internal/bundlespec), [`atomic/internal/bundlemirror/`](../../atomic/internal/bundlemirror), [`atomic/internal/embedded/`](../../atomic/internal/embedded), [`atomic/internal/templaterender/`](../../atomic/internal/templaterender), [`atomic/internal/claudeinstall/`](../../atomic/internal/claudeinstall), [`atomic/cmd/bundle-mirror/`](../../atomic/cmd/bundle-mirror), [`atomic/cmd/render-templates/`](../../atomic/cmd/render-templates), [`docs/spec/uninstall.md`](../../docs/spec/uninstall.md), [`docs/design/uninstall.md`](../../docs/design/uninstall.md) | Template render (commands + agents) → bundle embed → install/uninstall into ~/.claude | [`.claude/project/signals/bundle.md`](signals/bundle.md) |
| doctor | [`atomic/internal/doctor/`](../../atomic/internal/doctor), [`atomic/internal/validate/`](../../atomic/internal/validate), [`atomic/internal/cliusage/`](../../atomic/internal/cliusage), [`atomic/internal/manifestcheck/`](../../atomic/internal/manifestcheck), [`atomic/internal/updatedoctor/`](../../atomic/internal/updatedoctor), [`atomic/internal/profile/`](../../atomic/internal/profile), [`docs/spec/atomic-doctor.md`](../../docs/spec/atomic-doctor.md), [`docs/spec/atomic-validate.md`](../../docs/spec/atomic-validate.md), [`docs/spec/validate-artifact-cli-flags.md`](../../docs/spec/validate-artifact-cli-flags.md), [`docs/spec/verify-gate-validate.md`](../../docs/spec/verify-gate-validate.md), [`docs/spec/atomic-update-doctor.md`](../../docs/spec/atomic-update-doctor.md), [`docs/spec/user-profile.md`](../../docs/spec/user-profile.md), [`docs/design/user-profile.md`](../../docs/design/user-profile.md), [`docs/design/validate-artifact-cli-flags.md`](../../docs/design/validate-artifact-cli-flags.md), [`docs/design/verify-gate-validate.md`](../../docs/design/verify-gate-validate.md) | 11-check integrity suite + static validation (A1: artifact CLI-flag lint) + CLI surface table + post-update auto-fire + user profile + code-index health | [`.claude/project/signals/doctor.md`](signals/doctor.md) |
| workflow | [`commands/autopilot.md`](../../commands/autopilot.md), [`commands/atomic-plan.md`](../../commands/atomic-plan.md), [`commands/gather-evidence.md`](../../commands/gather-evidence.md), [`commands/subagent-implementation.md`](../../commands/subagent-implementation.md), [`commands/subagent-diagnose.md`](../../commands/subagent-diagnose.md), [`commands/atomic-setup.md`](../../commands/atomic-setup.md), [`commands/atomic-improve.md`](../../commands/atomic-improve.md), [`commands/commit.md`](../../commands/commit.md), [`commands/undo-commit.md`](../../commands/undo-commit.md), [`commands/_templates/`](../../commands/_templates), [`templates/shared/worktree-setup.md`](../../templates/shared/worktree-setup.md), [`agents/atomic-implementer.md`](../../agents/atomic-implementer.md), [`agents/atomic-reviewer.md`](../../agents/atomic-reviewer.md), [`agents/atomic-investigator.md`](../../agents/atomic-investigator.md), [`agents/atomic-strategist.md`](../../agents/atomic-strategist.md), [`skills/atomic-tdd/`](../../skills/atomic-tdd), [`skills/atomic-verify/`](../../skills/atomic-verify), [`skills/atomic-commit/`](../../skills/atomic-commit), [`skills/atomic-review/`](../../skills/atomic-review), [`skills/atomic-debug/`](../../skills/atomic-debug), [`skills/atomic-visual-options/`](../../skills/atomic-visual-options) | Plan → gather-evidence → implement → review → ship → retrospective lifecycle; `/autopilot` runs it hands-off; `/commit` is the unified ship verb (ask-don't-enumerate) | [`.claude/project/signals/workflow.md`](signals/workflow.md) |
| config | [`commands/follow-up.md`](../../commands/follow-up.md), [`commands/remind-me.md`](../../commands/remind-me.md), [`commands/git-cleanup.md`](../../commands/git-cleanup.md), [`commands/watch-ci.md`](../../commands/watch-ci.md), [`atomic/internal/coldprompt/`](../../atomic/internal/coldprompt), [`atomic/internal/config/`](../../atomic/internal/config), [`atomic/internal/hooks/`](../../atomic/internal/hooks), [`atomic/internal/reminder/`](../../atomic/internal/reminder), [`atomic/internal/followups/`](../../atomic/internal/followups), [`atomic/internal/prompt/`](../../atomic/internal/prompt), [`atomic/internal/selfupdate/`](../../atomic/internal/selfupdate) | User config, state dir (profile.md, config.toml), session hooks, reminders, follow-ups, self-update; cold-op briefs (git-cleanup, claude-merge) embedded in binary via coldprompt package | [`.claude/project/signals/config.md`](signals/config.md) |
| docs-meta | [`output-styles/atomic.md`](../../output-styles/atomic.md), [`skills/atomic-documentation/`](../../skills/atomic-documentation), [`skills/atomic-prose/`](../../skills/atomic-prose), [`commands/documentation.md`](../../commands/documentation.md), [`.claude/rules/authoring/`](../rules/authoring), [`docs/spec/documentation-skill-split.md`](../../docs/spec/documentation-skill-split.md), [`docs/spec/documentation-as-maintenance.md`](../../docs/spec/documentation-as-maintenance.md) | Two-voice taxonomy, diff-driven surface routing, prose style, design axioms | [`.claude/project/signals/docs-meta.md`](signals/docs-meta.md) |
| wiki | [`commands/refresh-wiki.md`](../../commands/refresh-wiki.md), [`atomic/internal/wiki/`](../../atomic/internal/wiki), [`docs/spec/wiki.md`](../../docs/spec/wiki.md), [`docs/spec/wiki-buckets.md`](../../docs/spec/wiki-buckets.md), [`docs/design/wiki.md`](../../docs/design/wiki.md), [`docs/design/wiki-buckets.md`](../../docs/design/wiki-buckets.md), [`docs/reference/wiki-workflow.md`](../../docs/reference/wiki-workflow.md), [`docs/reference/concepts.md`](../../docs/reference/concepts.md) (Wikis section), [`docs/credits.md`](../../docs/credits.md) | Cross-repo knowledge layer: scan → classify → stale → LLM refresh (repo summaries + bucket synthesis → knowledge pages); ship-time dirty marker; session-start nudge | [`.claude/project/signals/wiki.md`](signals/wiki.md) |
| code-intel | [`atomic/internal/codeintel/`](../../atomic/internal/codeintel), [`atomic/cmd/embedded-sql-admission/`](../../atomic/cmd/embedded-sql-admission), [`docs/spec/code-intel-engine.md`](../../docs/spec/code-intel-engine.md), [`docs/spec/code-intel-extraction.md`](../../docs/spec/code-intel-extraction.md), [`docs/spec/code-intel-resolution.md`](../../docs/spec/code-intel-resolution.md), [`docs/spec/code-intel-substrate.md`](../../docs/spec/code-intel-substrate.md), [`docs/spec/code-intel-query.md`](../../docs/spec/code-intel-query.md), [`docs/spec/code-intel-surfaces.md`](../../docs/spec/code-intel-surfaces.md), [`docs/spec/sql-language-support.md`](../../docs/spec/sql-language-support.md), [`docs/spec/embedded-sql-extraction.md`](../../docs/spec/embedded-sql-extraction.md), [`docs/spec/embedded-sql-language-expansion.md`](../../docs/spec/embedded-sql-language-expansion.md), [`docs/spec/code-intel-integration.md`](../../docs/spec/code-intel-integration.md), [`docs/spec/code-mcp-per-repo.md`](../../docs/spec/code-mcp-per-repo.md), [`docs/design/code-intel-engine.md`](../../docs/design/code-intel-engine.md), [`docs/design/code-intel-integration.md`](../../docs/design/code-intel-integration.md), [`docs/design/embedded-sql-extraction.md`](../../docs/design/embedded-sql-extraction.md), [`docs/design/embedded-sql-language-expansion.md`](../../docs/design/embedded-sql-language-expansion.md), [`docs/guides/code-intel-mcp.md`](../../docs/guides/code-intel-mcp.md), [`docs/reference/code-intel.md`](../../docs/reference/code-intel.md), [`scripts/code-eval/`](../../scripts/code-eval) | Tree-sitter/wazero symbol graph + SQLite store + `atomic code` CLI verbs + MCP server + embedded-SQL extraction (20 host languages) + agent integration | [`.claude/project/signals/code-intel.md`](signals/code-intel.md) |
| serve | [`atomic/internal/serve/`](../../atomic/internal/serve), [`atomic/internal/mdlink/extract_links.go`](../../atomic/internal/mdlink/extract_links.go), [`atomic/internal/frontmatter/frontmatter.go`](../../atomic/internal/frontmatter/frontmatter.go), [`docs/spec/atomic-serve.md`](../../docs/spec/atomic-serve.md), [`docs/design/atomic-serve.md`](../../docs/design/atomic-serve.md), [`docs/reference/serve.md`](../../docs/reference/serve.md) | Local read-only HTTP server: Obsidian-style shell (breadcrumb + md|code search, left nav, middle content [page|system] toggle, right rail YAML Properties + mini-graph + OUT/IN); goldmark render + in-body wikilink resolution, Cytoscape+ELK, federated code search, code-file modal | [`.claude/project/signals/serve.md`](signals/serve.md) |

## Cross-cutting

**Deterministic substrate**: [`.claude/project/deterministic-signals.md`](deterministic-signals.md) — written by `atomic signals scan`. Never edit by hand.

**@-ref wiring**: for this repo, `@.claude/project/signals.md` lives in [`claude.local.md`](../../claude.local.md) (not [`CLAUDE.md`](../../CLAUDE.md) — [`CLAUDE.md`](../../CLAUDE.md) is the bundle source and must not carry project-specific paths). Only `signals.md` is `@-ref`'d. `deterministic-signals.md` is NOT `@-ref`'d — it can be thousands of lines on large repos and would blow up context. The inferrer reads it when needed; sessions do not.

**Doctor refs check updated**: [`atomic/internal/doctor/checks_refs.go`](../../atomic/internal/doctor/checks_refs.go) (hash `477404b`) now checks only for `@.claude/project/signals.md`. Prior contract (requiring both `deterministic-signals.md` and `signals.md`) is superseded per spec change 2026-05-26.

**Doctor hooks scope bug fixed**: `checks_hooks.go` `checkHooks` now passes `$HOME` as scopeRoot to `RunCheckHooksWith` — not `~/.claude`. Passing `~/.claude` caused `hooks.IsInstalled` to look for `~/.claude/.claude/settings.json` (double [`.claude`](..) segment). `RunCheckHooksWith(scopeRoot)` is exported for tests; `checks_hooks_internal_test.go` added for package-internal test coverage.

**Hooks inline-command migration**: [`atomic/internal/hooks/hooks.go`](../../atomic/internal/hooks/hooks.go) `Install` now registers the literal string `atomic hooks session-start` directly in `settings.json`'s `SessionStart` hook array — no wrapper script written. Legacy installs that registered `~/.claude/hooks/session-start-reminders.sh` are detected via `IsInstalled` `drifted=true` return and migrated away by `migrateLegacy` on the next `Install` call. `legacyScriptName` / `legacyHooksSubdir` constants retained solely for this migration path.

**`go:embed all:bundle` requirement (bundle)**: [`commands/_templates/`](../../commands/_templates) starts with `_` — excluded by the default embed glob. `all:bundle` overrides this. Any new underscore-prefixed directory under `embedded/bundle/` needs this same consideration.

**Pipeline order is load-bearing (bundle)**: `make render` must precede `make bundle`. The pre-commit hook stages 1 and 2 enforce this order; stage 1 re-stages both [`commands/`](../../commands) and [`agents/`](../../agents) after render. CI runs the same two drift gates in order. Running only `make bundle` after editing a template embeds stale command or agent outputs.

**Agent template partial pipeline**: [`templates/agents/`](../../templates/agents) now mirrors [`templates/commands/`](../../templates/commands) — agent source files are rendered, not directly edited. Eight agent-specific partials now live in [`templates/shared/`](../../templates/shared): `agent-tdd-signals`, `agent-signals-output`, `agent-shared-rules`, `agent-search-tooling` (carries one-line bridge naming code-intel as top search tier), `agent-implementer-workflow`, `agent-atomic-voice` (response-voice rule — injected into all 5 agent templates), `agent-code-intel` (`atomic code` verb guidance: lead with `explore` for orientation, then targeted verbs; graceful degradation), `agent-yagni` (YAGNI 7-step ladder — composed into `atomic-implementer`, `atomic-reviewer`, `atomic-strategist`). `atomic-implementer.md` receives `agent-code-intel` via `agent-implementer-workflow` and `agent-yagni` directly. `atomic-investigator.md`, `atomic-reviewer.md`, and `atomic-signals-inferrer.md` include `agent-code-intel` directly. `atomic-reviewer.md` and `atomic-strategist.md` include `agent-yagni` directly. `make render` produces `agents/*.md`; `make bundle` embeds them. Editing `agents/*.md` directly is overwritten on next render — edit `templates/agents/*.md` instead.

**Domain partitioning basis**: domains are vertical slices by feature concern, not horizontal layers by file type. Each domain file answers: "if I'm working on X, what artifacts, what CLI code, and what docs do I need?"

**Artifact additions checklist**: adding any new command/agent/skill requires updating the artifact file, [`CLAUDE.md`](../../CLAUDE.md), [`README.md`](../../README.md), relevant [`docs/reference/`](../../docs/reference) tables, `docs/spec/<topic>.md` if non-trivial, cross-references in other artifacts, running `make render` and `make bundle`, and `/refresh-signals`. See [`claude.local.md`](../../claude.local.md) for the full checklist with per-row guidance.

**`/refresh-signals` is the single idempotent entry point**: `/initialize-signals` was removed (commit 4011b30). `/refresh-signals` handles both first-run init and subsequent refreshes. The `atomic-signals` skill was removed and absorbed into `atomic-signals-inferrer` — the agent now owns the full pipeline (scan + infer + wire). `/refresh-signals` Step 4 auto-indexes on cold index without prompting.

**[`CLAUDE.md`](../../CLAUDE.md) compressed (bundle domain)**: "Two voices", "Subagents available for dispatch", and "Project signals" sections removed from [`CLAUDE.md`](../../CLAUDE.md). "Simplicity first" principle rewritten as a 7-step YAGNI ladder. Auto-index policy added to "Code-intel engine" section (index is cheap and idempotent — `atomic code index` runs automatically without permission prompt). "Two voices" content folded into [`output-styles/atomic.md`](../../output-styles/atomic.md) Boundaries section. Agent/skill discovery now relies on the harness agent roster per session, not a CLAUDE.md registry section. `validate/config.go` rule C1 (which checked for the now-removed "Subagents available for dispatch" section) was retired.

**`atomic validate artifacts` (rule A1, doctor domain)**: [`atomic/internal/cliusage/`](../../atomic/internal/cliusage) is the single source of truth for the [`atomic`](../../atomic) command surface — verb-path tokens, accepted `--flags`, descriptions. `main.go` renders `--help` from it; `validate/artifacts.go` uses it to lint artifact citations for unknown verbs/flags. Adding or removing a verb or flag requires updating `cliusage.go`; failing to do so causes `atomic validate artifacts` to either false-positive (flag a valid citation) or false-negative (miss an invalid one). The A1 rule fires as part of whole-repo `atomic validate` (no subcommand) and as `atomic validate artifacts [paths...]`. The `atomic-verify` skill integration spec is at [`docs/spec/verify-gate-validate.md`](../../docs/spec/verify-gate-validate.md).

**`atomic signals stale` exit-code contract**: returns `(StaleInfo, error)`. Exit 0 = fresh; exit 1 = stale (stdout has imperative evidence lines); exit 2 = hard error (binary absent, scan failed, etc.). Spec: [`docs/spec/atomic-binary.md`](../../docs/spec/atomic-binary.md).

**`<atomic>` block-aware CLAUDE.md handling (bundle domain)**: [`atomic/internal/claudeinstall/atomicblock.go`](../../atomic/internal/claudeinstall/atomicblock.go) parses the single `<atomic>...</atomic>` block in CLAUDE.md (line-anchored tag matching, mirrors the `<wikis>` block parser). `planArtifact` now classifies CLAUDE.md as `ActionBlockReplaced` (in-place block swap, user content outside preserved byte-for-byte) when both on-disk and embedded carry a parseable block; `ActionMergeRequired` fires only when no parseable block exists — the install output directs the user to run `atomic prompt claude-merge` in a Claude Code session (not a slash command). `atomicBlocksEqual` is used by `claudeinstall.Diff` and doctor check 1 to treat a merged file with a current block as a match. Specs amended: [`docs/spec/atomic-binary.md`](../../docs/spec/atomic-binary.md), [`docs/spec/install-workflow.md`](../../docs/spec/install-workflow.md).

**Uninstall feature**: [`atomic/internal/claudeinstall/`](../../atomic/internal/claudeinstall) contains `snapshot.go`, `uninstall.go` + tests. Spec at [`docs/spec/uninstall.md`](../../docs/spec/uninstall.md), design at [`docs/design/uninstall.md`](../../docs/design/uninstall.md). Implements `atomic claude uninstall` — reads pre-install snapshot, computes restore plan, LLM-merges modified files.

**Repo-local commands ([`.claude/commands/`](../commands))**: [`.claude/commands/triage-issues.md`](../commands/triage-issues.md) — a project-local (gitignored) slash command for issue triage; paired with [`scripts/triage-scan.sh`](../../scripts/triage-scan.sh). Not bundled, not installed to `~/.claude`.

**VitePress docs site**: not part of the Go build or embedded bundle. [`package.json`](../../package.json) / [`.vitepress/config.mts`](../../.vitepress/config.mts) are purely for the public docs site. `docs.yml` workflow deploys it.

**Typed follow-ups (config domain)**: [`atomic/internal/followups/entry.go`](../../atomic/internal/followups/entry.go) defines `Kind` type (`KindFinding`/`KindPlan`). Missing `kind` parses as `KindFinding` (back-compat). `--severity` optional when `--kind plan`. `Render` places `## 📋 plans` first; plans are exempt from staleness. Plans are filed via `atomic followups add --kind plan`. Spec: [`docs/spec/typed-followups.md`](../../docs/spec/typed-followups.md).

**Wiki feature (wiki domain)**: [`atomic/internal/wiki/`](../../atomic/internal/wiki) implements `atomic wiki scan`, `atomic wiki stale` (reports `DRIFT`/`STALE` lines for membership drift, artifact content drift, and `STALE bucket <name>` for buckets with non-empty diffs), `atomic wiki stamp` (writes `reflects_rev`/`reflects:` fingerprints — code-only, model never writes fingerprints; `--knowledge --sources` mode writes `sources:` YAML list on knowledge pages), `atomic wiki bucket add|list|diff|promote` (SHA-256 manifest engine: `BucketDiff` writes `current`, `PromoteBucket` rotates baseline→previous; `bucketDiffReadOnly` used by list/stale — no side effects), and `atomic wiki mark-dirty` (touches `.dirty` on ship). `/refresh-wiki` dispatches `atomic-signals-inferrer` in wiki-output mode using `atomic signals scan --out <tmp>` to avoid writing into the target repo; also dispatches in bucket-synthesis mode per bucket with a non-empty diff, writing topic-keyed pages under `wiki/knowledge/`. Bucket manifests live at `wiki/.buckets/<name>/`. The `<wiki-buckets>` block in `wiki/index.md` is machine-managed (mirrors `<wiki-scan>`); on first `/refresh-wiki` with no block, a bucket-add offer fires once — blank response records `declined="true"`. `## Capture surfaces` section in realm [`CLAUDE.md`](../../CLAUDE.md) written once on first `bucket add`. The `signals-gate` partial invokes `atomic wiki mark-dirty` after signals refresh. Session-start hook calls `wiki.CheckStaleness` best-effort with 30-day default. The `claude-merge` cold-op brief ([`atomic/internal/coldprompt/briefs/claude-merge.md`](../../atomic/internal/coldprompt/briefs/claude-merge.md)) preserves the `<wikis>` block verbatim on merge. `classifyMembers` four-rule precedence: (1) prior `summarized` + summary file exists on disk → keep; (2) [`.claude/project/signals.md`](signals.md) exists → `indexed` (signals win over summary); (3) `discoverSummary` finds `wiki/repos/<name>.md` or `wiki/repos/<name>/` with at least one `.md` → `summarized`; (4) else → `pending`. Rule 3 (disk-discovery, added 2026-06-11) makes `summarized` reachable on the closing re-scan after `/refresh-wiki` writes summaries. **Known gap (filed)**: [`atomic/internal/hooks/hooks_test.go`](../../atomic/internal/hooks/hooks_test.go) `TestSessionStart_EmptyReminders` + two siblings read the real `~/.claude/CLAUDE.md` `<wikis>` block — fails on machines with registered dirty wikis. Fix: inject HOME or CLAUDE.md path as a seam in `internal/hooks` tests.

**`atomic-documentation` skill is two-mode**: maintenance mode (fires during ship verbs — flags stale/incomplete only) and authoring mode (`/documentation` — full discovery, gap detection, content generation). `atomic docs scan` / `atomic docs stale` are binary subcommands that support it.

**Reuse-first / YAGNI reflex (workflow domain)**: [`agents/atomic-reviewer.md`](../../agents/atomic-reviewer.md) Step 4 (over-engineering check) flags hand-rolled stdlib replacements, duplicate helpers, and one-implementation abstractions — names the concrete built-in/stdlib replacement in the finding. [`agents/atomic-implementer.md`](../../agents/atomic-implementer.md) carries the YAGNI ladder via `agent-yagni` partial. [`agents/atomic-strategist.md`](../../agents/atomic-strategist.md) also carries `agent-yagni`. [`CLAUDE.md`](../../CLAUDE.md) Principles block carries "Simplicity first (YAGNI)" as a 7-step ladder (global principle); the same ladder is in [`templates/shared/agent-yagni.md`](../../templates/shared/agent-yagni.md) — kept verbatim in sync (CLAUDE.md is not rendered; duplication is manual). [`commands/atomic-plan.md`](../../commands/atomic-plan.md) design-doc phase surfaces YAGNI as a review dimension. [`skills/atomic-review/SKILL.md`](../../skills/atomic-review/SKILL.md) includes the same over-engineering flag for conversational PR reviews.

**Stuck-fix escalation + suppression awareness (workflow domain)**: `/subagent-implementation` Step C carries a stuck-fix escalator — after 2 consecutive `CHANGES_REQUESTED` rounds on the same root-cause, surfaces `/pressure-test @<spec>` + `atomic-strategist` RCA offer; never auto-dispatched (axiom 3). `atomic-reviewer` gains Step 5 suppression-pattern check. Spec: [`docs/spec/stuck-fix-escalation.md`](../../docs/spec/stuck-fix-escalation.md). Design: [`docs/design/stuck-fix-escalation.md`](../../docs/design/stuck-fix-escalation.md).

**Config schema v1 (current)**: `output.intensity` key removed. Schema is now `output.signals.max_depth` (int, default 3) + `update.run_doctor` (bool, default `true`) — two keys only. `checks_config.go` (doctor domain) validates against this schema.

**`atomic update` artifact auto-refresh (config domain)**: after binary swap, `main.go:runUpdate` re-execs the new binary as `<exe> claude update --no-update-check` by default — no managed-install detection gate; anyone running `atomic update` is assumed to want the whole product current. `artifactRefreshArgs(hooksInstalled)` appends `--no-hooks` when no session-start hook is registered, so refresh never first-registers hooks. `--skip-claude-update` opts out entirely. Failure warns on stderr; never blocks update exit code. The out-of-sync nudge is removed. `detectManagedInstall`, `maybeRefreshArtifacts`, `resolveIfManagedGate`, and `claudeinstall/managed.go` (DetectManaged/ManagedState) were all deleted. Spec: [`docs/spec/atomic-update-doctor.md`](../../docs/spec/atomic-update-doctor.md) (Artifact auto-refresh contract section). `cliusage.go` update verb entry has `--skip-claude-update`.

**Path-scoped rule [`rules/specs/spec-currency.md`](../../rules/specs/spec-currency.md)**: bundled rule with `paths: ["docs/spec/**/*.md", "docs/design/**/*.md"]`. Auto-loads when touching spec or design files. Bundle now contains 3 path-scoped rules (typescript, python, specs).

**[`output-styles/atomic.md`](../../output-styles/atomic.md) updated**: intensity levels removed. Style organized as clarity goal + drop-list + pattern + Auto-Clarity escape hatch + structure-over-prose + subagents section + Boundaries section. Boundaries section now carries the Two-voices taxonomy (atomic style = how Claude talks; files follow their own voice via `atomic-prose` / `atomic-documentation`). The "Two voices" section was removed from [`CLAUDE.md`](../../CLAUDE.md) and consolidated into `atomic.md`. `agent-atomic-voice` partial carries response-voice rule into each agent.

**[`.claude/rules/authoring/`](../rules/authoring)**: repo-only rules (never bundled) — `axioms.md`, `agent-config.md`, `claude-code-refs.md`, `prompting.md`. Path-scoped to load only when editing artifact sources.

**User profile v2**: [`atomic/internal/profile/`](../../atomic/internal/profile) — detection registry of 57 entries, parallel `DetectAll`, `RenderEnvironmentSection` with `<deterministic lastcheck=YYYY-MM-DD>`, atomic-write `Refresh`, `RefreshIfStale` 7-day window via session-start hook. Doctor category 10 gained `lastcheck` freshness check at `profileStaleDays = 30`. Spec: [`docs/spec/user-profile.md`](../../docs/spec/user-profile.md).

**Code-intel engine (code-intel domain)**: [`atomic/internal/codeintel/`](../../atomic/internal/codeintel) — ~16-package subsystem. `atomic code` verb dispatches via `main.go:codeAction` → `codeintel/cli/code.go:RunCode`. Engine facade at `engine/engine.go` wraps db, pool, orchestrator, resolution pipeline, graph manager, searcher, context builder. tsbinding sub-module owns the wazero/tree-sitter WASM binding. SQLite index at `<projectRoot>/.claude/.atomic-index/atomic.db` (gitignored). MCP server at `mcp/server.go` with 8 tools (`atomic_code_*`), tool-gating for tiny repos (< 500 files → 3 tools). SQL extractor at `extraction/standalone/sql.go` (standalone, no tree-sitter). **Embedded SQL extraction** (shipped — 20 host languages): `indexer/embedded_sql_postpass.go` (CP2) + `extraction/standalone/embedded_sql.go` (CP1 gate/entry) + `extraction/python_literals.go` (CP3) + `extraction/typescript_literals.go` (CP4) cover Go/Python/TypeScript/TSX. **New (language expansion)**: `extraction/embedded_literals.go` provides `HarvestEmbeddedLiterals` — a single generic harvester for 16 additional languages (C, C++, C#, Java, JavaScript, Kotlin, Lua, Luau, Objective-C, Pascal, PHP, Ruby, Rust, Scala, Swift, Dart); per-language `EmbeddedLiteralConfig` table in `indexer/embedded_literals_config.go`. Node kinds probed from live grammars; do not alter without re-probing (spec: [`docs/spec/embedded-sql-language-expansion.md`](../../docs/spec/embedded-sql-language-expansion.md)). Fixture corpus at [`scripts/code-eval/fixtures/embedded-sql-multilang/`](../../scripts/code-eval/fixtures/embedded-sql-multilang). All embedded edges carry `Provenance:"embedded"`. Eval tool: `cmd/embedded-sql-admission/`. Watch stubbed (`ErrWatchNotImplemented`). Branding hard rule: reference TypeScript engine's product name must never appear in code, comments, identifiers, strings, or output. See [`.claude/project/signals/code-intel.md`](signals/code-intel.md) for full package breakdown.

**Code-intel integration (code-intel-integration feature)**: `agent-code-intel` partial ([`templates/shared/agent-code-intel.md`](../../templates/shared/agent-code-intel.md)) is composed into 4 agents (implementer via `agent-implementer-workflow`, investigator, reviewer, signals-inferrer directly; strategist does not receive it directly). Contract: index present → lead with `atomic code explore "<query>"` for area orientation (one-shot bundled context digest); then `search`/`callers`/`callees`/`impact` for targeted symbol questions; bounded queries (one explore question or one symbol at a time); graceful degradation to sg/grep on any failure (binary absent, DB missing, query error) — no error surfaced to user. **Auto-index policy** (updated): `/refresh-signals` Step 4 runs `atomic code index` on cold index without prompting — indexing is cheap and idempotent, assume the user wants it. `/subagent-implementation` code-intel section does the same. `/autopilot` also auto-indexes without prompting. Warm index: always `atomic code sync`. Doctor check 11 (`checks_code_index.go`) reports index health. MCP registration is manual opt-in via `.mcp.json` only — no auto-register on `atomic claude install`. Spec: [`docs/spec/code-intel-integration.md`](../../docs/spec/code-intel-integration.md). Design: [`docs/design/code-intel-integration.md`](../../docs/design/code-intel-integration.md). Guide: [`docs/guides/code-intel-mcp.md`](../../docs/guides/code-intel-mcp.md).

**`atomic serve` FE rework (serve domain)**: [`atomic/internal/serve/`](../../atomic/internal/serve) — UI is now a single persistent Obsidian-style shell: top bar (breadcrumb + `md|code` search), left nav, middle pane with `[page|system]` toggle, right rail. New handlers: `rail_handler.go` (`NewRailHandler`, `GET /rail/<relpath>`) composites four htmx OOB fragments (`#rail-props-content` YAML frontmatter Properties, `#rail-out-content` outbound links, `#rail-in-content` backlinks + orphan note, `#rail-graph-content` Cytoscape mini-graph via `/graph/data?node=<page>&depth=1`); `search_md.go` (`NewMdSearchHandler`, `GET /search/md?q=`) does literal case-insensitive substring grep of all `.md` files (cap 50, snippet 120 chars). `graph.go` gained `Edge.CodeFile bool` (non-.md source files rendered as `/file/` links, not broken) and `Graph.nodeSet map[string]bool` for O(1) `Has()`. `codeexplorer.go` gained `GET /code/file?path=<relpath>` using `GetNodesInFile`. `parseFlags` normalizes relative target-dir to absolute via `filepath.Abs`. Standalone `/graph` page removed (FE3) — system view is the `[system]` middle-pane toggle loading `/graph/data` directly. `/health` renamed to `/status` (SC6: health is ambient, not the landing). `/healthz` liveness probe retained. `Run` / `RunWithContext` entry points unchanged; link graph built once at startup via `BuildLinkGraph`. wiki exports still required: `wiki.BucketDiffReadOnly`, `wiki.ReadBucketEntries`, `wiki.FileSHA256`, `wiki.ResolveFingerprint`. `mdlink.ExtractLinks` in [`atomic/internal/mdlink/extract_links.go`](../../atomic/internal/mdlink/extract_links.go) unchanged. Spec: [`docs/spec/atomic-serve.md`](../../docs/spec/atomic-serve.md). Design: [`docs/design/atomic-serve.md`](../../docs/design/atomic-serve.md). Reference: [`docs/reference/serve.md`](../../docs/reference/serve.md).

**In-body wikilink rendering (serve domain)**: [`atomic/internal/serve/wikilink.go`](../../atomic/internal/serve/wikilink.go) adds a goldmark inline parser (`wikilinkInlineParser`, priority 150) + AST node (`wikilinkNode`) + renderer (`wikilinkRenderer`) for Obsidian-style `[[page]]` / `[[page|alias]]` links. `wikilinkResolverFromGraph` reads the focused page's already-computed `Graph.Outbound` edges (filtered to `mdlink.Wikilink` kind) — no second resolution pass; the body and rail agree by construction. `render.go` gained `RenderMarkdownWithGraph(src, root, pageRelPath, g)` as the new production entry point; `context_handler.go` calls it for all page renders. CSS in `assets/app.css`: `.md-content a.wikilink`, `.md-content a.wikilink-ambiguous`, `.md-content .wikilink-broken`. Spec SC4 clause added in [`docs/spec/atomic-serve.md`](../../docs/spec/atomic-serve.md) change-log entry 2026-06-16.

**YAML frontmatter Properties slot (serve domain)**: [`atomic/internal/frontmatter/frontmatter.go`](../../atomic/internal/frontmatter/frontmatter.go) gained `ParseOrdered(input []byte) ([]KV, body []byte, error)` — a key-order-preserving sibling of `Parse`; both now call the shared private `splitFrontmatter` helper. `render.go`:`renderMarkdown` strips the frontmatter block via `frontmatter.Parse` before goldmark (falls through to original src on parse error). `rail_handler.go` calls `frontmatter.ParseOrdered` to emit `#rail-props-content` as the first of four OOB fragments; scalars pass through as-is, `[]any` list values are comma-joined, nested maps are skipped; the slot is CSS-hidden (`#rail-props:has(.rail-slot-content:empty){display:none}`) when no frontmatter is present. `templates/layout.html` has `#rail-props` at the top of `#right-rail`. Spec SC4 + FE-SC2 clauses + change-log entry 2026-06-17 in [`docs/spec/atomic-serve.md`](../../docs/spec/atomic-serve.md).

**Language breakdown updated (5.5.0)**: C LOC absent — `tsbinding/src/` vendored-on-demand. JavaScript LOC dropped from 10034 → 4179 (htmx 2→4 is a smaller bundle). CSS LOC up to 2922 (FE polish). HTML LOC up to 1653 (layout.html additions). Go up to 132868 / 385 files (elixir/erlang/phoenix spec files, tsbinding sub-module additions).

**Code-intel deleted-file pruning (code-intel domain, 5.6.0)**: [`atomic/internal/codeintel/indexer/orchestrator.go`](../../atomic/internal/codeintel/indexer/orchestrator.go) `pruneDeleted` runs after every `IndexAll` and `Sync`. Builds an O(1) membership set of on-disk relative paths, queries `GetAllFiles`, then for each path absent from disk runs one `db.WithTx`: `Tx.DeleteNodesByFile` (FK-cascade removes edges) + `Tx.DeleteUnresolvedRefsByFile` + `Tx.DeleteFile`. Crash-safe: each orphan is handled in its own transaction — a mid-run crash leaves the remaining files intact. `IndexPaths` does NOT prune (it receives an explicit subset and would wrongly delete every out-of-subset file). `Tx.DeleteFile` is the new method added to [`atomic/internal/codeintel/db/tx.go`](../../atomic/internal/codeintel/db/tx.go) for this purpose.

**Per-repo MCP cwd-independence (code-intel domain, 5.6.0)**: `engine.NewWithDBPath(projectRoot, dbPath)` in [`atomic/internal/codeintel/engine/engine.go`](../../atomic/internal/codeintel/engine/engine.go) accepts an explicit db path, decoupling source root from index location. [`atomic/internal/codeintel/mcp/daemon.go`](../../atomic/internal/codeintel/mcp/daemon.go) `RunDaemon(ctx, sourceRoot, dbPath, now, watchInterval)` uses this to start daemons that are cwd-independent — `atomic --repo <abs-path> code mcp` works from any directory. Socket keyed via `SocketPathFromDB(dbPath)` (next to the db file) so multiple per-repo daemons run concurrently without port conflicts. Default `SyncInterval = 10s`. Spec: [`docs/spec/code-mcp-per-repo.md`](../../docs/spec/code-mcp-per-repo.md).

**OKF-alignment (serve + wiki domains)**: `graph.go` gained `Graph.nodeTypes map[string]string` populated by `resolveNodeType` (frontmatter `type` key case-insensitive via `frontmatterTypeToClass` → path-convention `/repos/`|`/concerns/`|`/knowledge/` → default "page"); `g.NodeType(relPath)` is O(1). `/graph/data` JSON carries `type` on each node element; Cytoscape CSS selectors in `layout.html` color nodes by OKF concept type with a type legend/filter on the system graph. `resolveRootRelative` is a new shared helper called by both `resolveMarkdownLink` and `resolvePageHref` for bundle-root-relative `/path.md` links (OKF §5.1 form) — prevents the link graph and render path from diverging. `rail_handler.go` `propKV.IsURL bool` makes http(s) frontmatter values (including `resource:`) render as `<a target="_blank">` anchors in the Properties slot. `wiki.go` `buildMembersSection` now emits OKF §6 listing form (`- [name](target) - description`) via `DeriveMemberDescription` (exported, resolution: frontmatter `description:` → first prose line → ""; 120-char truncation). [`commands/refresh-wiki.md`](../../commands/refresh-wiki.md) concern authoring writes `type: Concern` + `description`. [`agents/atomic-signals-inferrer.md`](../../agents/atomic-signals-inferrer.md) bucket-synthesis B3 writes `type: Knowledge` + `description` + bundle-relative cross-links on knowledge pages. [`docs/design/okf-alignment.md`](../../docs/design/okf-alignment.md) and [`docs/spec/okf-alignment.md`](../../docs/spec/okf-alignment.md) document the design and contract.

**`atomic serve` FE polish (serve domain)**: nav-item links now render amber (`var(--amber)`) to match right-rail OUT/IN links; left inset set to 20px (section headers stay at 8px, leaves indent inside them); visited/hover/active states pinned explicitly — independent of global `a:visited` color. Brand element upgraded from `<span>` to `<a href={{.LandingURL}}>` with htmx nav attributes (clicking the logo navigates home without a full reload). `chromaHighlight` and `chromaHighlightLines` no longer call `lexers.Analyse` (content-based guessing) — unknown fence language falls through to plaintext (no spurious highlighting). Search JS in `layout.html` now boots with a `window.__atomicSearchBooted` guard and uses delegated `document.addEventListener('click')` listeners so they survive htmx 4 history-restore body swaps; `btn-graph` click similarly migrated to a delegated listener. `htmx:after:swap` handler resets `#main-pane.scrollTop = 0` on content swaps; modal resets `scrollTop = 0` on open.

**`atomic-visual-options` skill (workflow domain)**: [`skills/atomic-visual-options/SKILL.md`](../../skills/atomic-visual-options/SKILL.md) auto-fires on visual-comparison phrases ("show me a few options", "mock up some variants", "compare these layouts", etc.) and is invoked just-in-time by `/atomic-plan` Diverge phase when a design question passes the see-it-over-read-it gate. Renders 2–4 side-by-side variants per dimension as a single throwaway self-contained HTML file; user picks by typing terminal codes (e.g. `A2 B3`); chosen codes recorded in the design doc. Scope: visual choices (layout, color, spacing, hierarchy, diagram) — not conceptual or text decisions.
