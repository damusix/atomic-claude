---
# generated by /refresh-wiki — do not hand-edit; regenerated every refresh
paths: ["atomic/internal/config/**", "atomic/internal/scratchpad/**", "atomic/internal/migrate/**", "atomic/internal/selfupdate/**", "atomic/internal/hooks/**", "atomic/internal/reminder/**", "atomic/internal/followups/**", "atomic/internal/prompt/**", "atomic/internal/repoctx/**", "atomic/internal/where/**", "atomic/internal/repoinit/**", "atomic/internal/coldprompt/**", "atomic/internal/doctemplate/**", "atomic/internal/dockerinit/**", "atomic/cmd/atomic/main.go"]
---

Domain: config. User and repo config, state at `~/.atomic`, hooks, follow-ups, self-update.

- Map: docs/wiki/config.md
- Contracts: docs/spec/atomic-state-and-config.md
- Reference: docs/reference/conventions.md

Consult the map before changing behavior here. Behavior changes stale the pages above. Renames or removals stale mentions beyond them: grep the old name across docs/ before shipping.
